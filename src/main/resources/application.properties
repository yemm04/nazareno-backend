spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/nazareno_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:whiskas123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

app.jwt.secret=${JWT_SECRET:cambia-esto-por-una-cadena-larga-y-aleatoria-de-al-menos-32-caracteres}